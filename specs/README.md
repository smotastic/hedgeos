# Specifications

Feature specifications belong in directories following this pattern:

```text
specs/<domain>/<id>-<slug>/
```

Create specifications from a cleared Wayfinder map with `/to-spec`. Turn an
approved specification into implementation tickets with `/to-tickets` before
writing product code.
